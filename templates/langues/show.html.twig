{% extends 'base.html.twig' %}

{% block title %}Langues{% endblock %}

{% block body %}
    <h1>Langues</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ langue.id }}</td>
            </tr>
            <tr>
                <th>Langues</th>
                <td>{{ langue.Langues }}</td>
            </tr>
            <tr>
                <th>Notion</th>
                <td>{{ langue.Notion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('langues_index') }}">back to list</a>

    <a href="{{ path('langues_edit', {'id': langue.id}) }}">edit</a>

    {{ include('langues/_delete_form.html.twig') }}
{% endblock %}
