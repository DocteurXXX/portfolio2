{% extends 'base.html.twig' %}

{% block title %}Liens{% endblock %}

{% block body %}
    <h1>Liens</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lien.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ lien.Nom }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ lien.Lien }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('liens_index') }}">back to list</a>

    <a href="{{ path('liens_edit', {'id': lien.id}) }}">edit</a>

    {{ include('liens/_delete_form.html.twig') }}
{% endblock %}
